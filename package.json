{
  "name": "c",
  "version": "1.0.0",
  "description": "Plataforma de productos",
  "main": "script.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "api": "json-server --watch ./database/db.json --port 3001"
  },
  "author": "Gabriela Ferrer",
  "license": "ISC",
  "dependencies": {
    "json-server": "^1.0.0-beta.3"
  }
}
